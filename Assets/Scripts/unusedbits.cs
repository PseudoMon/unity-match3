/*
    private Vector2[] usableGrids = new Vector2[10 * 10];

    // Start is called before the first frame update
    void Start()
    {
        int arrayId = 0;

        for (int row = 0; row < 10; row++)
        {
            for (int cell = 0; cell < 10; cell++)
            {
                usableGrids[arrayId] = new Vector2(cell, row)
                arrayId += 1;
            }
        }

        
    }
    */